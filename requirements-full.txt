# requirements-full.txt
# =====================
# Full dependencies: backend + Streamlit UI + vector DB + embeddings + pySigma.
# Install: pip install -r requirements-full.txt

# --- Core (same as requirements-min.txt) ---

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9

rank-bm25>=0.2.2
numpy>=1.26.0
pypdf>=4.0.0
networkx>=3.2
pyyaml>=6.0
python-dateutil>=2.8.2

pytest>=7.4.0
httpx>=0.26.0

# --- Extended deps ---

# Storage — SQLAlchemy is imported by db.py but not strictly needed (stdlib sqlite3)
sqlalchemy>=2.0.0

# Vector DB + embeddings (for hybrid retrieval)
chromadb>=0.4.22
sentence-transformers>=2.5.0

# Extended PDF parsing
pdfplumber>=0.10.0

# Sigma formal validation
pysigma>=0.11.0

# LLM providers (optional — system defaults to "mock")
openai>=1.10.0
anthropic>=0.20.0

# Frontend
streamlit>=1.30.0
pyvis>=0.3.2

# Utilities
requests>=2.31.0
tldextract>=5.1.0
